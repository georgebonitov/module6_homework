const exponentiation = (x, y) => {
    const result = x ** y;
    return result;
}

let numberOne = Number(prompt());
let numberTwo = Number(prompt());

console.log(exponentiation(numberOne,numberTwo));